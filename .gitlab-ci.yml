# =============================================================================
# GitLab CI/CD Pipeline for Terraform Infrastructure
# =============================================================================
#
# Stages organized by environment for clear UI:
#   Stage 1: shared-euw1 (VPC, Route53, ECR)
#   Stage 2: dev-euw1 (IAM, EKS)
#   Stage 3: prod-euw1 (IAM, EKS)
#   Stage 4: destroy (manual only)
#
# =============================================================================

stages:
  - shared-euw1
  - dev-euw1
  - prod-euw1
  - destroy

variables:
  TF_VERSION: "1.8"
  GITLAB_TF_ADDRESS: "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/terraform/state"

# =============================================================================
# Base Template
# =============================================================================
.terraform:
  image:
    name: hashicorp/terraform:${TF_VERSION}
    entrypoint: [""]
  id_tokens:
    GITLAB_OIDC_TOKEN:
      aud: sts.amazonaws.com
  before_script:
    - |
      export TF_HTTP_ADDRESS="${GITLAB_TF_ADDRESS}/${TF_STATE_NAME}"
      export TF_HTTP_LOCK_ADDRESS="${TF_HTTP_ADDRESS}/lock"
      export TF_HTTP_UNLOCK_ADDRESS="${TF_HTTP_ADDRESS}/lock"
      export TF_HTTP_USERNAME="gitlab-ci-token"
      export TF_HTTP_PASSWORD="${CI_JOB_TOKEN}"
      export TF_HTTP_LOCK_METHOD="POST"
      export TF_HTTP_UNLOCK_METHOD="DELETE"

      apk add --no-cache aws-cli > /dev/null 2>&1

      CREDENTIALS=$(aws sts assume-role-with-web-identity \
        --role-arn "${AWS_ROLE_ARN}" \
        --role-session-name "gitlab-ci-${CI_JOB_ID}" \
        --web-identity-token "${GITLAB_OIDC_TOKEN}" \
        --duration-seconds 3600 \
        --query 'Credentials.[AccessKeyId,SecretAccessKey,SessionToken]' \
        --output text)

      export AWS_ACCESS_KEY_ID=$(echo $CREDENTIALS | awk '{print $1}')
      export AWS_SECRET_ACCESS_KEY=$(echo $CREDENTIALS | awk '{print $2}')
      export AWS_SESSION_TOKEN=$(echo $CREDENTIALS | awk '{print $3}')
    - cd ${TF_DIR}
    - terraform init
  retry:
    max: 2
    when: [runner_system_failure, stuck_or_timeout_failure]

# =============================================================================
# STAGE: shared-euw1 (Shared Account - eu-west-1)
# =============================================================================
"vpc:plan":
  extends: .terraform
  stage: shared-euw1
  variables:
    TF_DIR: aws/shared-euw1/vpc
    TF_STATE_NAME: shared-euw1-vpc
    AWS_ROLE_ARN: "arn:aws:iam::265245191272:role/GitLabRunnerRole"
  script:
    - terraform validate
    - terraform plan -out=tfplan
  artifacts:
    paths: [aws/shared-euw1/vpc/tfplan]
    expire_in: 7 days
  rules:
    - if: $CI_COMMIT_BRANCH == "master" || $CI_PIPELINE_SOURCE == "merge_request_event"

"vpc:apply":
  extends: .terraform
  stage: shared-euw1
  variables:
    TF_DIR: aws/shared-euw1/vpc
    TF_STATE_NAME: shared-euw1-vpc
    AWS_ROLE_ARN: "arn:aws:iam::265245191272:role/GitLabRunnerRole"
  script:
    - terraform apply -auto-approve tfplan
  dependencies: ["vpc:plan"]
  needs: ["vpc:plan"]
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: manual

"route53:plan":
  extends: .terraform
  stage: shared-euw1
  variables:
    TF_DIR: aws/shared-euw1/route53
    TF_STATE_NAME: shared-euw1-route53
    AWS_ROLE_ARN: "arn:aws:iam::265245191272:role/GitLabRunnerRole"
  script:
    - terraform validate
    - terraform plan -out=tfplan
  artifacts:
    paths: [aws/shared-euw1/route53/tfplan]
    expire_in: 7 days
  rules:
    - if: $CI_COMMIT_BRANCH == "master" || $CI_PIPELINE_SOURCE == "merge_request_event"

"route53:apply":
  extends: .terraform
  stage: shared-euw1
  variables:
    TF_DIR: aws/shared-euw1/route53
    TF_STATE_NAME: shared-euw1-route53
    AWS_ROLE_ARN: "arn:aws:iam::265245191272:role/GitLabRunnerRole"
  script:
    - terraform apply -auto-approve tfplan
  dependencies: ["route53:plan"]
  needs: ["route53:plan"]
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: manual

"ecr:plan":
  extends: .terraform
  stage: shared-euw1
  variables:
    TF_DIR: aws/shared-euw1/ecr
    TF_STATE_NAME: shared-euw1-ecr
    AWS_ROLE_ARN: "arn:aws:iam::265245191272:role/GitLabRunnerRole"
  script:
    - terraform validate
    - terraform plan -out=tfplan
  artifacts:
    paths: [aws/shared-euw1/ecr/tfplan]
    expire_in: 7 days
  rules:
    - if: $CI_COMMIT_BRANCH == "master" || $CI_PIPELINE_SOURCE == "merge_request_event"

"ecr:apply":
  extends: .terraform
  stage: shared-euw1
  variables:
    TF_DIR: aws/shared-euw1/ecr
    TF_STATE_NAME: shared-euw1-ecr
    AWS_ROLE_ARN: "arn:aws:iam::265245191272:role/GitLabRunnerRole"
  script:
    - terraform apply -auto-approve tfplan
  dependencies: ["ecr:plan"]
  needs: ["ecr:plan"]
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: manual

# =============================================================================
# STAGE: dev-euw1 (Dev Account - eu-west-1)
# =============================================================================
"dev:iam:plan":
  extends: .terraform
  stage: dev-euw1
  variables:
    TF_DIR: aws/dev-euw1/iam
    TF_STATE_NAME: dev-euw1-iam
    AWS_ROLE_ARN: "arn:aws:iam::975050047325:role/DevDeployRole"
  script:
    - terraform validate
    - terraform plan -out=tfplan
  artifacts:
    paths: [aws/dev-euw1/iam/tfplan]
    expire_in: 7 days
  rules:
    - if: $CI_COMMIT_BRANCH == "master" || $CI_PIPELINE_SOURCE == "merge_request_event"

"dev:iam:apply":
  extends: .terraform
  stage: dev-euw1
  variables:
    TF_DIR: aws/dev-euw1/iam
    TF_STATE_NAME: dev-euw1-iam
    AWS_ROLE_ARN: "arn:aws:iam::975050047325:role/DevDeployRole"
  script:
    - terraform apply -auto-approve tfplan
  dependencies: ["dev:iam:plan"]
  needs: ["dev:iam:plan"]
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: manual

"dev:eks:plan":
  extends: .terraform
  stage: dev-euw1
  variables:
    TF_DIR: aws/dev-euw1/eks
    TF_STATE_NAME: dev-euw1-eks
    AWS_ROLE_ARN: "arn:aws:iam::975050047325:role/DevDeployRole"
  script:
    - terraform validate
    - terraform plan -out=tfplan
  artifacts:
    paths: [aws/dev-euw1/eks/tfplan]
    expire_in: 7 days
  rules:
    - if: $CI_COMMIT_BRANCH == "master" || $CI_PIPELINE_SOURCE == "merge_request_event"

"dev:eks:apply":
  extends: .terraform
  stage: dev-euw1
  variables:
    TF_DIR: aws/dev-euw1/eks
    TF_STATE_NAME: dev-euw1-eks
    AWS_ROLE_ARN: "arn:aws:iam::975050047325:role/DevDeployRole"
  script:
    - terraform apply -auto-approve tfplan
  dependencies: ["dev:eks:plan"]
  needs: ["dev:eks:plan"]
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: manual

# =============================================================================
# STAGE: prod-euw1 (Prod Account - eu-west-1)
# =============================================================================
"prod:iam:plan":
  extends: .terraform
  stage: prod-euw1
  variables:
    TF_DIR: aws/prod-euw1/iam
    TF_STATE_NAME: prod-euw1-iam
    AWS_ROLE_ARN: "arn:aws:iam::624755517249:role/ProdDeployRole"
  script:
    - terraform validate
    - terraform plan -out=tfplan
  artifacts:
    paths: [aws/prod-euw1/iam/tfplan]
    expire_in: 7 days
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: manual

"prod:iam:apply":
  extends: .terraform
  stage: prod-euw1
  variables:
    TF_DIR: aws/prod-euw1/iam
    TF_STATE_NAME: prod-euw1-iam
    AWS_ROLE_ARN: "arn:aws:iam::624755517249:role/ProdDeployRole"
  script:
    - terraform apply -auto-approve tfplan
  dependencies: ["prod:iam:plan"]
  needs: ["prod:iam:plan"]
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: manual

"prod:eks:plan":
  extends: .terraform
  stage: prod-euw1
  variables:
    TF_DIR: aws/prod-euw1/eks
    TF_STATE_NAME: prod-euw1-eks
    AWS_ROLE_ARN: "arn:aws:iam::624755517249:role/ProdDeployRole"
  script:
    - terraform validate
    - terraform plan -out=tfplan
  artifacts:
    paths: [aws/prod-euw1/eks/tfplan]
    expire_in: 7 days
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: manual

"prod:eks:apply":
  extends: .terraform
  stage: prod-euw1
  variables:
    TF_DIR: aws/prod-euw1/eks
    TF_STATE_NAME: prod-euw1-eks
    AWS_ROLE_ARN: "arn:aws:iam::624755517249:role/ProdDeployRole"
  script:
    - terraform apply -auto-approve tfplan
  dependencies: ["prod:eks:plan"]
  needs: ["prod:eks:plan"]
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: manual

# =============================================================================
# STAGE: destroy (Manual only)
# =============================================================================
"dev:eks:destroy":
  extends: .terraform
  stage: destroy
  variables:
    TF_DIR: aws/dev-euw1/eks
    TF_STATE_NAME: dev-euw1-eks
    AWS_ROLE_ARN: "arn:aws:iam::975050047325:role/DevDeployRole"
  script:
    - terraform destroy -auto-approve
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: manual

"prod:eks:destroy":
  extends: .terraform
  stage: destroy
  variables:
    TF_DIR: aws/prod-euw1/eks
    TF_STATE_NAME: prod-euw1-eks
    AWS_ROLE_ARN: "arn:aws:iam::624755517249:role/ProdDeployRole"
  script:
    - terraform destroy -auto-approve
  rules:
    - if: $CI_COMMIT_BRANCH == "master"
      when: manual
