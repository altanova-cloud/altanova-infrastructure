services:
  tf:
    image: hashicorp/terraform:1.7
    volumes:
      - ../../../:/project
    working_dir: /project/aws/environments/dev-app-account
    environment:
      - AWS_ACCESS_KEY_ID
      - AWS_SECRET_ACCESS_KEY
      - AWS_SESSION_TOKEN